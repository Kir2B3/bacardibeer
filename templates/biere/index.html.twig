{% extends 'base.html.twig' %}

{% block title %}Biere index{% endblock %}

{% block body %}
    <h1>Biere index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Marque</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for biere in bieres %}
            <tr>
                <td>{{ biere.id }}</td>
                <td>{{ biere.nom }}</td>
                <td>{{ biere.marque.nom }}</td>
                <td>
                    <a href="{{ path('app_biere_show', {'id': biere.id}) }}">show</a>
                    <a href="{{ path('app_biere_edit', {'id': biere.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_biere_new') }}">Create new</a>
{% endblock %}
