{% extends 'base.html.twig' %}

{% block title %}Bar{% endblock %}

{% block body %}
    <h1>Bar</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ bar.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ bar.nom }}</td>
            </tr>
            <tr>
                <th>Licence</th>
                <td>{{ bar.licence }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_bar_index') }}">back to list</a>

    <a href="{{ path('app_bar_edit', {'id': bar.id}) }}">edit</a>

    {{ include('bar/_delete_form.html.twig') }}
{% endblock %}
